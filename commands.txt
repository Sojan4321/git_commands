git config --global user.name "sojan">> configure git with our username

git config --global user.email "sojanthomas202002@gmail.com" configure git with our emailid

configuration requires while pushing towards a public repository in github from a local repository

git status >> used to check the ststus of .git in the current directory

git init >> to initialise git in the current directory (help to track the changes in our directory)

* if we add,delete a file it will track it as a change in the directory

u >> represents untracked (file is there not yet commited or not added to staging area)

A >> file that added to staging area

M >> file being modified after committed

git add intro.txt >>intro.txt added to staging area

git add .  >> all files added to staging area 

git commit -m "my first commit

git clone  <repository url> for cloning repository from github

